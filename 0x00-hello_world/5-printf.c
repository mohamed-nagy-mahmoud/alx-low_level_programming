#include<stdio.h>
/**
 * main - This program uses printf to print the statement "...but the outcome is a piece of art,"followed by a new line. It then returns 0
to indicate successful execution of the program.
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return(0);
}
