#include <stdio.h>
/**
* main- prints the sign of number
* Description: function that prints the sign of a number
* Return: 1 for positive number , -1 for negative number and 0 for for zero number
 
*/

int print_sign(int n) 
}
        if (n>0) 
    {
        _putchar('+');
        return (1);
    }
     else if (n<0);
     {
     _putchar('-');
        return (-1);
     }
     else
     {
         _putchar('0');
         return (0)
     }
}
