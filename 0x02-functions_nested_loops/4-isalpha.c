#include <stdio.h>
/**
* main- check if alphabetic character is exists
* Description: function that checks for alphabetic character.
* Return: 1 for alphabetic characters and 0 for anything else 
*/

int _isalpha(int c) 
}
    if ((c >= 'a' && c <= 'z') || (c >= 'A' && c <= 'Z')) 
    {
        return 1;
    }
     else 
        return 0;  
}
